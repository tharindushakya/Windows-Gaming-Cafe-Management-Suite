<UserControl x:Class="GamingCafe.POS.Windows.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0">
            <TextBlock Text="Station Name" />
            <TextBox x:Name="StationNameBox" Width="300" />
            <TextBlock Text="Connection String" Margin="0,8,0,0" />
            <TextBox x:Name="ConnectionStringBox" Width="600" />
            <TextBlock Text="Database Provider" Margin="0,8,0,0" />
            <ComboBox x:Name="ProviderBox" Width="200">
                <ComboBoxItem>Auto</ComboBoxItem>
                <ComboBoxItem>SqlServer</ComboBoxItem>
                <ComboBoxItem>Postgres</ComboBoxItem>
            </ComboBox>
            <TextBlock Text="Postgres example: Host=localhost;Database=GamingCafeDB;Username=gamingcafe;Password=secret" FontStyle="Italic" FontSize="11" Foreground="Gray" Margin="0,6,0,0" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="SaveBtn" Content="Save" Padding="8" Margin="0,0,8,0" />
            <Button x:Name="CancelBtn" Content="Cancel" Padding="8" />
        </StackPanel>
    </Grid>
</UserControl>
