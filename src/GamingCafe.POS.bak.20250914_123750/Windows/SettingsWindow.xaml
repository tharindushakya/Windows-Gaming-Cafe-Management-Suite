<Window x:Class="GamingCafe.POS.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="POS Settings" Height="600" Width="800" 
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,10"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50">
            <Grid>
                <TextBlock Text="⚙️ POS System Settings" 
                          FontSize="20" FontWeight="Bold" 
                          Foreground="White" 
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Center"/>
            </Grid>
        </Border>
        
        <!-- Content -->
        <TabControl Grid.Row="1" Margin="20">
            <!-- General Settings -->
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">
                        <GroupBox Header="Station Information" Margin="0,0,0,20">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Row="0" Grid.Column="0" Content="Station Name:" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" x:Name="StationNameTextBox" Margin="5" Padding="8"/>
                                
                                <Label Grid.Row="1" Grid.Column="0" Content="Station ID:" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" x:Name="StationIdTextBox" Margin="5" Padding="8"/>
                                
                                <Label Grid.Row="2" Grid.Column="0" Content="Receipt Printer:" VerticalAlignment="Center"/>
                                <ComboBox Grid.Row="2" Grid.Column="1" x:Name="PrinterComboBox" Margin="5" Padding="8">
                                    <ComboBoxItem Content="Default Printer"/>
                                    <ComboBoxItem Content="No Printer"/>
                                </ComboBox>
                            </Grid>
                        </GroupBox>
                        
                        <GroupBox Header="Tax Settings" Margin="0,0,0,20">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Row="0" Grid.Column="0" Content="Tax Rate (%):" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" x:Name="TaxRateTextBox" Margin="5" Padding="8"/>
                                
                                <CheckBox Grid.Row="1" Grid.Column="1" x:Name="IncludeTaxCheckBox" Content="Include tax in displayed prices" Margin="5" VerticalAlignment="Center"/>
                            </Grid>
                        </GroupBox>
                        
                        <GroupBox Header="Display Settings">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Row="0" Grid.Column="0" Content="Currency Symbol:" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" x:Name="CurrencySymbolTextBox" Margin="5" Padding="8"/>
                                
                                <CheckBox Grid.Row="1" Grid.Column="1" x:Name="ShowStockLevelsCheckBox" Content="Show stock levels on product cards" Margin="5" VerticalAlignment="Center"/>
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- Database Settings -->
            <TabItem Header="Database">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">
                        <GroupBox Header="Connection Settings">
                            <StackPanel Margin="15">
                                <Label Content="Database Connection String:"/>
                                <TextBox x:Name="ConnectionStringTextBox" Margin="5" Padding="8" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Content="Test Connection" Style="{StaticResource ModernButton}" Background="#27AE60" Click="TestConnection_Click"/>
                                    <Button Content="Reset to Default" Style="{StaticResource ModernButton}" Background="#E74C3C" Click="ResetConnection_Click"/>
                                </StackPanel>
                                
                                <TextBlock x:Name="ConnectionStatusText" Margin="5,10,5,0" FontWeight="Bold"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <!-- Security Settings -->
            <TabItem Header="Security">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">
                        <GroupBox Header="Access Control">
                            <StackPanel Margin="15">
                                <CheckBox x:Name="RequirePasswordCheckBox" Content="Require password for settings access" Margin="5"/>
                                <CheckBox x:Name="RequireManagerApprovalCheckBox" Content="Require manager approval for refunds" Margin="5"/>
                                <CheckBox x:Name="LogAllTransactionsCheckBox" Content="Log all transactions for audit" Margin="5"/>
                            </StackPanel>
                        </GroupBox>
                        
                        <GroupBox Header="Session Settings" Margin="0,20,0,0">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Label Grid.Row="0" Grid.Column="0" Content="Session Timeout (minutes):" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" x:Name="SessionTimeoutTextBox" Margin="5" Padding="8"/>
                                
                                <CheckBox Grid.Row="1" Grid.Column="1" x:Name="AutoLogoutCheckBox" Content="Automatically logout on session timeout" Margin="5" VerticalAlignment="Center"/>
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
        
        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="#34495E" BorderThickness="0,1,0,0" BorderBrush="#2C3E50">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0">
                <Button Content="Save Settings" Style="{StaticResource ModernButton}" Background="#27AE60" Click="SaveSettings_Click"/>
                <Button Content="Cancel" Style="{StaticResource ModernButton}" Background="#95A5A6" Click="Cancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
